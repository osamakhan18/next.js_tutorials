import React from 'react'

export default function Register() {
  return (
    <div>Register</div>
  )
}


// to use the routes in a group  we make this it  in the  like (auth) which make it more 
// secure and clean

// we use the () because we want to make the routes inside it not the self folder like auth
